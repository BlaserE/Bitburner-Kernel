# Tools
The `tools` directory is only and ONLY for holding compile-time files, such as `NetscriptDefinitions.d.ts`.

It is excluded from the compiled output because, again, it serves no purpose outside of coding.

