# Protocol
The complexity of the protocol implementation for this kernel is high enough that it justifies a dedicated subdirectory.

The reason is that it should never leak outside selected surfaces.

This section will be expanded upon as the protocol is developed. 